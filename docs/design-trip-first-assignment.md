# 設計背景：便起点の車両割り当てモデル

## 何を変えたか

このシステムは**GTFS上の便を先に生成し、始発時刻になった時点で車両を割り当てる**方式（便起点方式）を採用しています。2026年8月に、それ以前の方式（車両起点方式）から全面的に置き換えました。

| | 旧方式（車両起点） | 現方式（便起点） |
|---|---|---|
| 起点 | GPSから営業開始・出発を検知する | GTFSの運行日カレンダーに基づき、その日運行する便をあらかじめ全て生成する |
| 便の特定 | 出発時刻から時刻表上の便を逆引きする | 便は既に存在し、始発時刻になったら車両を紐付けるだけ |
| 進捗の単位 | 車両単位（`vehicles`テーブルに便情報を持たせる） | **便 × 車両のassignment単位**（`trip_vehicle_assignments`） |

## なぜ変えたか

車両起点方式は「GPSが取れて初めてその便の存在を知る」ため、以下のような構造的な弱さがありました。

- GPSが欠落すると、便自体が存在しなかったことになる（時刻表としての便と実績が同じ行に同居していたため）
- 1台の車両が複数便の候補になり得る状況（乗務員交代・順延など）を表現しにくい
- 「どの便の実績か」と「その便が今どういう状態か」が分離できない

便を先に生成しておけば、GTFSの時刻表データと当日の実績データが最初から別テーブルに分かれ、GPSの有無に関わらず便自体は必ず存在します。

## 中核となる設計：進捗はassignment単位

進捗データを「車両単位」ではなく「(便 × 車両) の割り当て単位」で持つのが最大の設計判断です。

```
daily_trips（当日の便。例：8:00発）
  ├─ trip_vehicle_assignments(role=assigned)  ── 担当車両 ── trip_stop_progress（その車両の通過実績）
  ├─ trip_vehicle_assignments(role=candidate) ── 候補車両 ── trip_stop_progress（その車両の通過実績）
  └─ trip_vehicle_assignments(role=candidate) ── 候補車両 ── trip_stop_progress（その車両の通過実績）
```

- 候補車両にも担当車両と**まったく同じ運行処理**（通過判定・遅延計算・ETA計算）を行います。ただし利用者向けAPIには出しません。
- 担当車両が運行終了したら、始発時刻時点の候補から再割り当てします。候補は最初からその便に紐づけて実績を記録しているため、**担当への昇格は「どの割り当てを正とみなすか」の切り替えだけ**で済み、実績のコピーやマージは一切発生しません。
- 「最も進んでいる車両を採用する」というマージは**やってはいけません**。別経路をたまたま走っていた車両を誤って採用する事故につながります。距離が最も近い候補を採用するルールを守ってください（詳細は[vehicle-assignment.md](vehicle-assignment.md)）。
- `vehicles`は「観測されている物理車両」を表すだけで、便との紐付けを持ちません。運行終了しても行は削除せず`status='inactive'`にします（1台が複数便の候補になり得るため、削除するとGPSログがCASCADEで消えて他便の処理まで壊れます）。

## 移行の名残

`vehicles`テーブルの`business_start_time` / `departure_time` / `trip_id` / `trip_type` / `last_arrived_seq` / `delay_minutes`は、旧・車両起点方式の名残で未使用です。移行のロールバック余地のために列だけ残してあります。新しいコードから参照しないでください（詳細は[database.md](database.md)）。

## 関連ドキュメント

- 各処理ステップの実行順序: [../README.md](../README.md)（パイプライン概要）
- 割り当て判定条件の詳細: [vehicle-assignment.md](vehicle-assignment.md)
- 通過判定の詳細: [pass-detection.md](pass-detection.md)
- 運行終了・アーカイブの詳細: [trip-lifecycle.md](trip-lifecycle.md)
